# Purrplex

Prerequisites:
- pnpm: `npm i -g pnpm` + `pnpm setup`
- python

Running:
- Bootstrap: `pnpm i`
- Run: `pnpm dev`

Resetting:
- Clean: `pnpm clean`
- Reset: `pnpm reset` (DANGER: THIS WILL CLEAR ALL "PROGRESS" MADE)
